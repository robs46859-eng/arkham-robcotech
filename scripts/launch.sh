#!/bin/bash
# Deprecated launcher kept only to redirect operators to the supported deploy flow.

set -e

echo "scripts/launch.sh is deprecated."
echo "Use ./scripts/deploy-bot.sh --setup and ./scripts/deploy-bot.sh --deploy instead."
exit 1
